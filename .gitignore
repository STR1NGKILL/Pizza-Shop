# User-specific stuff:
/local.properties

# Directories
/.gradle/
/lib/
/build/
.idea

# Avoid ignoring Gradle wrapper jar file (.jar files are usually ignored)
!gradle-wrapper.jar

# Cache of project
.gradletasknamecache
